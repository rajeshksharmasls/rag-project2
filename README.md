Clone the repository: https://github.com/rajeshksharmasls/rag-project2
Copy .env.example to .env and fill in your API keys
Install dependencies:
pip install -r requirements.txt
Update config.yaml with your Pinecone index name and desired parameters
Place your documents in the data/ directory
Run the pipeline:
python main.py
